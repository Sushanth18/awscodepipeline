version: 0.2


phases:
  install:
    runtime-versions:
      java: openjdk8
  
  build:
    commands:
      - echo Build started on `date`
      - mvn clean install
    
artifacts:
  files: 
    - target/*.jar
    - scripts/*.sh
    - appspec.yml  
  name: WarDeployment
  discard-paths: yes
  
cache:
  paths:
    - '/root/.m2/**/*'
