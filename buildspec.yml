version: 0.2

phases:
   build:
    commands:
    - echo building the justiceleague-tracker
    - mvn clean install
artifacts:
  files:
  - target/*.jar
  - scripts/*.sh
  - appspec.yml
  discard-paths: yescache:
  paths:
  - '/root/.m2/**/*'
