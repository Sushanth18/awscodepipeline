version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
   build:
    commands:
    - echo building the justiceleague-tracker
    - mvn install
artifacts:
  files:
  - target/*.jar
  - scripts/*.sh
  - appspec.yml
  discard-paths: yescache:
  paths:
  - '/root/.m2/**/*'
